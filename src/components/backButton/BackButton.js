import React from "react";
import styles from "./BackButton.module.css";

const BackButton = () => {
  return <button className={styles.backToProductBtn}>Back to products</button>;
};

export default BackButton;
